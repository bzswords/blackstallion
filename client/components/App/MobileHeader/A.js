import styled from 'styled-components';

import NormalA from './../../shared/A';

const A = styled(NormalA)`
  font-family: 'Work Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
  display:block;
  line-height: 1;
`;

export default A;
