// import React from 'react';
// import { shallow } from 'enzyme';

// import BackgroundImageSection from '../index';

describe('<BackgroundImageSection />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
